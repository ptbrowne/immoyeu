Scraper for house prices in l'Ile d'Yeu.

## Installation

```
git clone 
pip install -r requirements.txt
```

## Usage

To refresh the data.csv file, launch the scrapy spider.

```
$ scrapy crawl scrapeprice
```
